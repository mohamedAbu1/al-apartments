'use client';

import { FormEvent, useEffect, useState } from 'react';
import { ArrowRight, Building2, FileText, LandPlot, MapPin, ShieldCheck } from 'lucide-react';
import SiteFooter from '../components/site-footer';
import SiteHeader from '../components/site-header';

export default function ListPropertyPage() {
  const [isLand, setIsLand] = useState(() => typeof window !== 'undefined' && new URLSearchParams(window.location.search).get('type') === 'land');
  const [category, setCategory] = useState(() => typeof window !== 'undefined' && new URLSearchParams(window.location.search).get('type') === 'land' ? 'Land' : 'Villa');
  const [sent, setSent] = useState(false);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState('');
  useEffect(() => { const land = new URLSearchParams(window.location.search).get('type') === 'land'; setIsLand(land); setCategory(land ? 'Land' : 'Villa'); }, []);
  const submit = async (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault(); setBusy(true); setError('');
    const form = new FormData(event.currentTarget);
    if (isLand) form.set('operation', 'For sale');
    const message = [`Listing request: ${form.get('title')}`, `Category: ${form.get('category')}`, `Operation: ${form.get('operation')}`, `Location: ${form.get('location')}`, `Phone: ${form.get('phone')}`, isLand ? `Land area: ${form.get('area')} m²` : '', isLand ? `Land use: ${form.get('landUse')}` : '', isLand ? `Documents: ${form.get('documents')}` : '', `Details: ${form.get('details')}`].filter(Boolean).join('\n');
    try {
      const response = await fetch('/api/inquiries', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ kind: 'contact', propertyTitle: form.get('title'), name: form.get('name'), email: form.get('email'), message }) });
      const result = await response.json();
      if (!response.ok) throw new Error(result.message || 'Unable to send listing request.');
      setSent(true);
    } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Unable to send listing request.'); }
    finally { setBusy(false); }
  };
  return <main className={`listing-page ${isLand ? 'listing-land-page' : 'listing-property-page'}`}><SiteHeader section={isLand ? 'land' : 'buy-home'}/><section className="listing-hero"><div><span className="eyebrow">{isLand ? 'LIST YOUR LAND' : 'LIST WITH AL-AROUM'}</span><h1>{isLand ? <>Put your land<br/><em>in the right hands.</em></> : <>Let your property<br/><em>find its people.</em></>}</h1><p>{isLand ? 'Share the essential details of your land opportunity and our team will review its location, use, and documentation.' : 'Introduce your property to guests, buyers, and investors looking for something with real character.'}</p></div><div className="listing-hero-stamp"><ShieldCheck size={24}/><strong>Human review</strong><small>Every request is checked by the Al-Aroum team before publication.</small></div></section><section className="listing-layout"><div className="listing-guidance"><span className="eyebrow">A CLEARER FIRST STEP</span><h2>{isLand ? <>Tell us about<br/><em>the land.</em></> : <>Tell us about<br/><em>the opportunity.</em></>}</h2><p>{isLand ? 'Share the location, area, intended use, and document status so we can prepare a credible land opportunity.' : 'Send the basics now. We will contact you for photos, documents, availability, and the details needed to prepare a complete listing.'}</p><div className="listing-points">{isLand ? <><span><LandPlot size={18}/><strong>Residential, agricultural & tourism land</strong></span><span><MapPin size={18}/><strong>Egypt locations welcome</strong></span><span><FileText size={18}/><strong>Document status reviewed clearly</strong></span></> : <><span><Building2 size={18}/><strong>Homes, villas & chalets</strong></span><span><FileText size={18}/><strong>Clear information before publishing</strong></span><span><MapPin size={18}/><strong>Egypt locations welcome</strong></span></>}</div></div>{sent ? <div className="listing-success" role="status"><ShieldCheck size={28}/><h2>Request received.</h2><p>Thank you. Our team will review the information and contact you shortly for the next step.</p><a href={isLand ? '/?mode=land' : '/?mode=buy-home'} className="primary-cta">Back to my section <ArrowRight size={15}/></a></div> : <form className="listing-form" onSubmit={submit}><h2>{isLand ? 'Land opportunity details' : 'Property details'}</h2><p className="listing-form-intro">Fields marked with * are required.</p><label>Your name *<input name="name" required placeholder="Full name"/></label><label>Email address *<input name="email" required type="email" placeholder="name@example.com"/></label><label>Phone number *<input name="phone" required placeholder="+20 ..."/></label><label>{isLand ? 'Land title *' : 'Property title *'}<input name="title" required placeholder={isLand ? 'e.g. Dahab coastal plot' : 'e.g. Sea view chalet'}/></label><div className="listing-form-row"><label>Category *<select name="category" value={category} onChange={(event) => setCategory(event.target.value)}>{isLand ? <><option value="Land">Residential land</option><option value="Agricultural land">Agricultural land</option><option value="Tourism land">Tourism land</option><option value="Commercial land">Commercial land</option></> : <><option value="Apartment">Apartment</option><option value="Villa">Villa</option><option value="Chalet">Chalet</option></>}</select></label><label>Operation *<select name="operation" defaultValue="For sale"><option>For sale</option><option>For rent</option></select></label></div>{isLand && <div className="listing-form-row"><label>Land area *<input name="area" required type="number" min="1" placeholder="e.g. 850"/></label><label>Land use *<select name="landUse" defaultValue="Residential"><option>Residential</option><option>Agricultural</option><option>Tourism</option><option>Commercial</option></select></label></div>}{isLand && <label>Documents status *<select name="documents" defaultValue="Available"><option>Available</option><option>In progress</option><option>Needs review</option></select></label>}<label>Location *<input name="location" required placeholder="City, area, governorate"/></label><label>{isLand ? 'Land details' : 'Tell us more'}<textarea name="details" required rows={5} placeholder={isLand ? 'Road access, zoning, documents, price expectation, and anything important...' : 'Area, rooms, price expectation, availability, documents, and anything important...'}/></label>{error && <p className="form-error" role="alert">{error}</p>}<button className="primary-cta" disabled={busy} type="submit">{busy ? 'Sending request...' : 'Send listing request'} <ArrowRight size={15}/></button><small className="listing-privacy">Your information is sent securely to the Al-Aroum team.</small></form>}</section><SiteFooter section={isLand ? 'land' : 'buy-home'}/></main>;
}
